'use client'

// Dev mode role switching is no longer needed with real auth.
// This component is kept as a placeholder but does nothing.

export function DevModeButton() {
  // Real authentication is now in place - no dev mode switching needed
  return null
}
